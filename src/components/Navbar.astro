---
import NavbarApp from './react/Navbar';
import {
	getLocalizedPath,
	getPageLocaleSwitchHref,
	resolvePageContextFromPath,
	type PageId,
} from '../data/i18n/routing';
import { defaultLocale, locales, type LocaleCode } from '../data/i18n/locales';
import { reviewsBasePath } from '../data/reviews';
import { siteConfig } from '../data/site';
import {
	getBlogBasePath,
	getBlogLocaleSwitchHref,
	isBlogPath,
} from '../data/blog/helpers';

interface Props {
	locale?: LocaleCode;
	pageId?: PageId;
}

const { locale = defaultLocale, pageId: pageIdProp } = Astro.props;
const pathContext = resolvePageContextFromPath(Astro.url.pathname);
const pageId = pageIdProp ?? pathContext.pageId ?? 'home';
const current = Astro.url.pathname;
const onBlog = isBlogPath(current);

const pageContext = onBlog
	? pathContext
	: {
			...pathContext,
			pageId: pathContext.pageId ?? pageId,
		};

function localeSwitchHref(targetLocale: LocaleCode): string {
	if (onBlog) return getBlogLocaleSwitchHref(current, targetLocale);
	return getPageLocaleSwitchHref(pageContext, targetLocale);
}

const hrefForLocale = Object.fromEntries(
	locales.map((l) => [l.code, localeSwitchHref(l.code)]),
) as Record<string, string>;

const localeMetas = locales.map((l) => ({
	code: l.code,
	name: l.name,
	nativeName: l.nativeName,
	hreflang: l.hreflang,
	region: l.region,
}));

const links = [
	{ id: 'home', labelKey: 'nav.home', href: getLocalizedPath('home', locale) },
	{ id: 'hacks', labelKey: 'nav.preview', edit: 'navPreview', href: getLocalizedPath('hacks', locale) },
	{ id: 'esp', labelKey: 'nav.esp', href: getLocalizedPath('overwatch-2-esp', locale) },
	{ id: 'aimbot', labelKey: 'nav.aimbot', href: getLocalizedPath('overwatch-2-aimbot', locale) },
	{ id: 'pricing', labelKey: 'nav.store', edit: 'navStore', href: getLocalizedPath('pricing', locale) },
	{ id: 'updates', labelKey: 'nav.status', edit: 'navStatus', href: getLocalizedPath('updates', locale) },
	{ id: 'forum', labelKey: 'nav.forum', edit: 'blogLabel', href: getBlogBasePath(locale) },
	{ id: 'reviews', labelKey: 'nav.reviews', edit: 'navReviews', href: reviewsBasePath },
];
---

<NavbarApp
	client:load
	locale={locale}
	siteName={siteConfig.name}
	checkoutUrl={siteConfig.checkoutUrl}
	currentPath={current}
	homeHref={getLocalizedPath('home', locale)}
	reviewsBasePath={reviewsBasePath}
	locales={localeMetas}
	hrefForLocale={hrefForLocale}
	links={links}
/>

<style is:global>
	.lang-switcher {
		position: relative;
	}

	.lang-switcher__toggle {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		height: 2.15rem;
		padding: 0 0.85rem;
		border: 1px solid var(--line);
		border-radius: 0;
		background: var(--bg-panel);
		color: var(--ink);
		font-size: 0.8125rem;
		font-weight: 600;
		letter-spacing: -0.01em;
		list-style: none;
		cursor: pointer;
		transition:
			color 0.15s ease,
			background 0.15s ease,
			border-color 0.15s ease;
	}

	.lang-switcher__toggle::-webkit-details-marker {
		display: none;
	}

	.lang-switcher__toggle:hover {
		border-color: var(--line-strong);
		background: var(--bg-elevated);
	}

	.lang-switcher__toggle svg {
		width: 0.9rem;
		height: 0.9rem;
		flex-shrink: 0;
		color: var(--accent);
		opacity: 0.9;
	}

	.lang-switcher__panel {
		position: absolute;
		left: 0;
		top: calc(100% + 0.45rem);
		z-index: 120;
		width: min(16.5rem, calc(100vw - 2rem));
		max-height: min(20rem, 60vh);
		overflow: auto;
		padding: 0.5rem;
		background: var(--bg-panel);
		border: 1px solid var(--line);
		border-radius: var(--radius-md);
		box-shadow: 0 12px 32px color-mix(in srgb, var(--ink) 12%, transparent);
	}

	.site-tools .lang-switcher__panel {
		left: auto;
		right: 0;
	}

	.lang-switcher__note {
		margin: 0 0 0.45rem;
		padding: 0.45rem 0.55rem;
		border-radius: var(--radius-sm);
		background: var(--bg-elevated);
		color: var(--ink-faint);
		font-size: 0.6875rem;
		line-height: 1.4;
	}

	.lang-switcher__list {
		margin: 0;
		padding: 0;
		list-style: none;
		display: grid;
		gap: 0.2rem;
	}

	.lang-switcher__link {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 0.1rem;
		padding: 0.55rem 0.65rem;
		border-radius: var(--radius-sm);
		color: var(--ink);
		text-decoration: none;
		transition: background 0.15s ease;
	}

	.lang-switcher__link:hover,
	.lang-switcher__link.is-current {
		background: color-mix(in srgb, var(--accent) 8%, var(--bg-elevated));
	}

	.lang-switcher__link.is-current {
		box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 18%, transparent);
	}

	.lang-switcher__native {
		display: block;
		font-size: 0.875rem;
		font-weight: 600;
		line-height: 1.25;
	}

	.lang-switcher__region {
		display: block;
		font-size: 0.6875rem;
		font-weight: 500;
		line-height: 1.2;
		color: var(--ink-faint);
	}

	.site-header {
		position: sticky;
		top: 0;
		z-index: 100;
		padding-top: var(--safe-top, env(safe-area-inset-top, 0px));
		background: color-mix(in srgb, var(--bg) 72%, transparent);
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
		border-bottom: 1px solid transparent;
		transition:
			background 0.2s ease,
			border-color 0.2s ease,
			box-shadow 0.2s ease;
	}

	.site-header.is-scrolled {
		background: color-mix(in srgb, var(--bg) 92%, transparent);
		border-bottom-color: var(--line-soft);
		box-shadow: 0 8px 24px color-mix(in srgb, var(--ink) 8%, transparent);
	}

	.site-header__bar {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		align-items: center;
		gap: 0.65rem;
		min-height: var(--header-h);
	}

	.site-header__menu-btn {
		display: none;
		align-items: center;
		justify-content: center;
		width: var(--touch-min, 2.75rem);
		height: var(--touch-min, 2.75rem);
		padding: 0;
		border: 1px solid var(--line);
		border-radius: var(--radius-sm);
		background: var(--bg-panel);
		color: var(--ink);
		flex-shrink: 0;
		transition:
			border-color 0.15s ease,
			background 0.15s ease,
			color 0.15s ease;
	}

	.site-header__menu-btn:hover {
		border-color: var(--line-strong);
		background: var(--bg-elevated);
		color: var(--accent);
	}

	.site-header__menu-icon {
		position: relative;
		display: block;
		width: 1.15rem;
		height: 0.85rem;
	}

	.site-header__menu-icon span {
		position: absolute;
		left: 0;
		width: 100%;
		height: 2px;
		border-radius: 1px;
		background: currentColor;
		transition:
			transform 0.2s ease,
			opacity 0.2s ease,
			top 0.2s ease;
	}

	.site-header__menu-icon span:nth-child(1) {
		top: 0;
	}

	.site-header__menu-icon span:nth-child(2) {
		top: calc(50% - 1px);
	}

	.site-header__menu-icon span:nth-child(3) {
		top: calc(100% - 2px);
	}

	.site-header.is-menu-open .site-header__menu-icon span:nth-child(1) {
		top: calc(50% - 1px);
		transform: rotate(45deg);
	}

	.site-header.is-menu-open .site-header__menu-icon span:nth-child(2) {
		opacity: 0;
	}

	.site-header.is-menu-open .site-header__menu-icon span:nth-child(3) {
		top: calc(50% - 1px);
		transform: rotate(-45deg);
	}

	.site-mobile-nav__backdrop {
		position: fixed;
		inset: 0;
		z-index: 90;
		border: 0;
		padding: 0;
		background: color-mix(in srgb, var(--bg) 55%, transparent);
		backdrop-filter: blur(4px);
		-webkit-backdrop-filter: blur(4px);
	}

	.site-mobile-nav__backdrop[hidden] {
		display: none !important;
	}

	.site-mobile-nav {
		position: fixed;
		top: calc(var(--header-h) + var(--safe-top, env(safe-area-inset-top, 0px)));
		left: 0;
		right: 0;
		bottom: 0;
		z-index: 95;
		overflow: auto;
		-webkit-overflow-scrolling: touch;
		background: var(--bg);
		border-top: 1px solid var(--line-soft);
		box-shadow: 0 16px 40px color-mix(in srgb, var(--ink) 16%, transparent);
	}

	.site-mobile-nav[hidden] {
		display: none !important;
	}

	.site-mobile-nav__inner {
		display: grid;
		gap: 1.5rem;
		padding-block: 1.25rem 1.75rem;
		padding-bottom: max(1.75rem, var(--safe-bottom, env(safe-area-inset-bottom, 0px)));
	}

	.site-mobile-nav__links {
		margin: 0;
		padding: 0;
		list-style: none;
		display: grid;
		gap: 0.35rem;
	}

	.site-mobile-nav__links a {
		display: flex;
		align-items: center;
		min-height: var(--touch-min, 2.75rem);
		padding: 0.65rem 0.85rem;
		border: 1px solid var(--line-soft);
		border-radius: var(--radius-sm);
		background: var(--bg-panel);
		color: var(--ink);
		font-size: 0.9375rem;
		font-weight: 600;
		letter-spacing: -0.01em;
		transition:
			border-color 0.15s ease,
			background 0.15s ease,
			color 0.15s ease;
	}

	.site-mobile-nav__links a:hover {
		border-color: var(--line-strong);
		color: var(--accent);
		background: var(--bg-elevated);
	}

	.site-mobile-nav__links a.is-active {
		border-color: color-mix(in srgb, var(--accent) 35%, var(--line));
		color: var(--accent);
		background: color-mix(in srgb, var(--accent) 10%, var(--bg-panel));
		box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 18%, transparent);
	}

	.site-mobile-nav__tools {
		display: grid;
		gap: 0.75rem;
		padding-top: 0.5rem;
		border-top: 1px solid var(--line-soft);
	}

	.site-mobile-nav__tools-label {
		margin: 0;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--ink-faint);
	}

	.site-mobile-nav__tools .lang-switcher__panel {
		position: static;
		width: 100%;
		max-height: min(16rem, 40vh);
		margin-top: 0.35rem;
		box-shadow: none;
	}

	.site-mobile-nav__tools .lang-switcher__toggle {
		width: 100%;
		justify-content: flex-start;
		min-height: var(--touch-min, 2.75rem);
	}

	.site-mobile-nav__tools .lang-switcher__toggle span {
		max-width: none;
	}

	.site-mobile-nav__buy {
		display: flex;
		align-items: center;
		justify-content: center;
		min-height: var(--touch-min, 2.75rem);
		padding: 0.75rem 1.25rem;
		border-radius: var(--radius-sm);
		background: var(--accent);
		color: #fafbfd;
		font-size: 0.9375rem;
		font-weight: 700;
		letter-spacing: -0.01em;
		transition: background 0.15s ease;
	}

	.site-mobile-nav__buy:hover {
		background: var(--accent-hover);
		color: #fafbfd;
	}

	.site-nav {
		display: flex;
		align-items: center;
		justify-content: flex-start;
		gap: 0;
		width: 100%;
		min-width: 0;
		padding: 0.28rem;
		border: 1px solid var(--line-soft);
		border-radius: 0;
		background: var(--bg-panel);
		box-shadow: 0 1px 2px color-mix(in srgb, var(--ink) 4%, transparent);
		overflow-x: auto;
		scrollbar-width: none;
		-ms-overflow-style: none;
		-webkit-overflow-scrolling: touch;
	}

	.site-nav::-webkit-scrollbar {
		display: none;
	}

	.site-nav a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex: 1 1 0;
		min-width: 0;
		padding: 0.48rem 0.65rem;
		border-radius: 0;
		font-size: 0.8125rem;
		font-weight: 500;
		letter-spacing: -0.01em;
		text-align: center;
		color: var(--ink-muted);
		white-space: nowrap;
		transition:
			color 0.15s ease,
			background 0.15s ease;
	}

	.site-nav a:hover {
		color: var(--accent);
		background: color-mix(in srgb, var(--accent) 6%, transparent);
	}

	.site-nav a.is-active {
		color: var(--accent);
		background: color-mix(in srgb, var(--accent) 10%, transparent);
		box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 18%, transparent);
	}

	.site-tools {
		display: flex;
		align-items: center;
		justify-content: flex-end;
		gap: 0.45rem;
		flex-shrink: 0;
	}

	.site-tools__lang {
		flex-shrink: 0;
	}

	.site-tools__lang .lang-switcher__toggle span {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
		max-width: 5.5rem;
	}

	.site-tools__buy {
		position: relative;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.35rem;
		min-height: 2.15rem;
		height: auto;
		padding: 0 1rem;
		border-radius: 0;
		background: var(--accent);
		color: #FAFBFD;
		font-size: 0.8125rem;
		font-weight: 600;
		letter-spacing: -0.01em;
		transition:
			background 0.15s ease,
			transform 0.15s ease;
	}

	.site-tools__buy svg {
		width: 0.9rem;
		height: 0.9rem;
	}

	.site-tools__buy:hover {
		background: var(--accent-hover);
		color: #FAFBFD;
		transform: translateY(-1px);
	}

	@media (max-width: 1024px) {
		.site-tools__lang .lang-switcher__toggle,
		.site-tools__buy {
			min-height: var(--touch-min, 2.75rem);
		}
	}

	@media (max-width: 760px) {
		.site-header__bar {
			grid-template-columns: auto 1fr auto;
			gap: 0.5rem;
		}

		.site-header__menu-btn {
			display: inline-flex;
		}

		.site-nav {
			display: none;
		}

		.site-tools {
			justify-self: end;
		}

		.site-tools__lang {
			display: none;
		}

		.site-tools__buy {
			padding-inline: 0.85rem;
			font-size: 0.8125rem;
		}
	}

	@media (max-width: 480px) {
		.site-header__bar {
			gap: 0.4rem;
		}

		.site-tools {
			gap: 0.35rem;
		}

		.site-tools__buy {
			padding-inline: 0.75rem;
		}
	}

	@media (max-width: 360px) {
		.site-tools__buy span {
			position: absolute;
			width: 1px;
			height: 1px;
			padding: 0;
			margin: -1px;
			overflow: hidden;
			clip: rect(0, 0, 0, 0);
			white-space: nowrap;
			border: 0;
		}

		.site-tools__buy {
			padding-inline: 0.7rem;
			min-width: var(--touch-min);
			justify-content: center;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.site-header,
		.site-nav a,
		.site-tools__buy {
			transition: none;
		}

		.site-tools__buy:hover {
			transform: none;
		}
	}
</style>
